const state = {
  // data should be update after successfull login
  user: {
    name: "",
  },
};

const getters = {
  getUser: (state) => state.user,
};
const actions = {};
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
